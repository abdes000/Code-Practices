public class Substraction extends Operators {
    void operate() {
        System.out.println("This is Substratcion");
    }

    int difference(int a, int b) {
        // if (a > 0 && b < 0) {
        // System.out.println(a - b);
        // } else if (a < 0 && b > 0) {
        // System.out.println(b - a);
        // } else {
        // System.out.println(a - b);
        // }
        return a - b;

    }
}
